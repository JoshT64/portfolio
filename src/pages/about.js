import * as React from "react";
import {headingStyles, listItemStyles, listStyles, pageStyles, docLinkStyle} from "."

export default function About(){
    return <main style={pageStyles}><h1 style={headingStyles}>About</h1>
    <ul style={listStyles}>
        <li style={listItemStyles}>Placeholder</li>
    </ul>
    </main>
}